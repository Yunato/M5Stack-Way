#include "driver.hpp"

Driver::Driver(void) {}

Driver::~Driver(void) {}

bool Driver::execute(void){
	while(true){
    // gyro
		// if(INVERT_GS_RATE < std::abs(gyroRate)){
		// 	return true;
		// }

    // button or bluetooth
    // break

    // dist

    // up tail
	}
	return false;

}
